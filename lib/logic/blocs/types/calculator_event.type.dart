enum FastCalculatorBlocEventType {
  advancedMode,
  asyncOperation,
  asyncOperationDone,
  asyncOperationFailed,
  clear,
  compute,
  computed,
  computeFailed,
  init,
  initialized,
  initFailed,
  patchValue,
  save,
  share,
}
